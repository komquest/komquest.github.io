## Cron minimalist survival kit:

```
Crontab located at /etc/crontab

* * * * * = execute every min

*/5 * * * * = execute every 5 min

min hour day_of_month month day_of_week
```

## List to see if you have a Cron file or not:

```
crontab -l
```
