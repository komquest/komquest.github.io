--> Install python3 upload server, so you can exfil via http. This has the same options as http.server 

https://github.com/Densaugeo/uploadserver

```
python3 -m pip install uploadserver
```

```
python3 -m uploadserver
```